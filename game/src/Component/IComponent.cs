namespace AlleyCat.Component;

/// <summary>
/// Represents a reusable capability object that can be attached to a holder and discovered through the component query
/// helpers. Components keep focused gameplay behaviours composable without requiring each holder type to expose every
/// capability directly.
/// </summary>
public interface IComponent
{
}
