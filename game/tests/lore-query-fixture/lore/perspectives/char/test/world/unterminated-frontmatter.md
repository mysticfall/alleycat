---
id: test.unterminated
title: Unterminated Scratch Page
essential: true

This scratch page opens a frontmatter block but never closes it, so read-time
triage must skip it with a warning instead of breaking the query.
