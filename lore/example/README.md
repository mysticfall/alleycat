# Example Lore Root

This is the sample lore root for the `example` game context.

The directory roles and source conventions are defined by the `lore-graph-compiler` skill. This sample demonstrates
those conventions but is not the schema authority.
